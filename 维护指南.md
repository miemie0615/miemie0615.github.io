# AstroPaper 博客 · 中文维护指南

> 本文档面向本项目的日常维护者，说明**在哪里改、改什么、怎么运行**。
> 项目基于 [AstroPaper](https://github.com/satnaing/astro-paper) 主题（Astro 7 框架），使用 **pnpm** 作为包管理器。
> 编写日期：2026-08-24。

---

## 目录

1. [环境要求](#1-环境要求)
2. [常用命令速查](#2-常用命令速查)
3. [整体目录结构](#3-整体目录结构)
4. [「我想改 XX，去哪个文件」速查表](#4-我想改-xx去哪个文件速查表)
5. [写文章 / 改文章](#5-写文章--改文章)
6. [改「关于」等独立页面](#6-改关于等独立页面)
7. [改站点信息、导航、社交链接](#7-改站点信息导航社交链接)
8. [改界面文字（导航栏 / 按钮等）](#8-改界面文字导航栏--按钮等)
9. [改样式、主题颜色、深浅色](#9-改样式主题颜色深浅色)
10. [字体说明（已改为本地离线字体）](#10-字体说明已改为本地离线字体)
11. [构建与部署](#11-构建与部署)
12. [常见问题 FAQ](#12-常见问题-faq)

---

## 1. 环境要求

| 工具 | 版本要求 | 说明 |
| --- | --- | --- |
| Node.js | **≥ 22.12.0** | 本机已安装 v22.19.0 ✅ |
| pnpm | 任意较新版本 | 本机已通过 npm 全局安装 v11.23.0 ✅ |

> ⚠️ 本项目锁定用 **pnpm**（根目录有 `pnpm-lock.yaml`）。请勿混用 npm / yarn 安装依赖，否则可能导致依赖树不一致。
>
> 若换了新电脑，pnpm 安装方式：`npm install -g pnpm`。

---

## 2. 常用命令速查

> 所有命令都在项目目录 `astro-paper-main/` 下执行。

| 命令 | 作用 | 何时用 |
| --- | --- | --- |
| `pnpm install` | 安装 / 更新依赖 | 首次拉代码、或改了 `package.json` 后 |
| `pnpm dev` | 启动本地开发服务器（默认 http://localhost:4321/ ） | 日常写文章、改样式时实时预览 |
| `pnpm build` | 生产构建，产物输出到 `dist/` | 上线前打包 |
| `pnpm preview` | 本地预览生产构建结果 | 打包后本地验证 |
| `pnpm sync` | 同步 Astro 内容类型 | 改了内容 schema 后类型报错时 |
| `pnpm format` | 用 Prettier 自动格式化全部代码 | 提交前统一代码风格 |
| `pnpm lint` | ESLint 检查代码 | 排查潜在问题 |

**启动开发服务器示例：**

```bash
pnpm dev
```

启动后浏览器打开 http://localhost:4321/ 即可预览。**改动源文件会自动热更新，无需手动刷新。**

> 停止开发服务器：在运行 `pnpm dev` 的终端里按 `Ctrl + C`。

---

## 3. 整体目录结构

```text
astro-paper-main/
├── astro.config.ts            # Astro 框架配置（集成、字体、markdown 处理等）
├── astro-paper.config.ts      # ★ 站点配置入口（标题/作者/社交链接/功能开关）—— 最常改
├── package.json               # 依赖与脚本命令
│
├── public/                    # 静态资源，原样拷贝到网站根目录
│   ├── favicon.svg            # 网站图标
│   └── default-og.jpg         # 默认社交分享大图（OG image）
│
└── src/
    ├── config.ts              # 读取 astro-paper.config.ts 的中转文件（一般不用动）
    ├── content.config.ts      # ★ 内容集合的字段规则（文章/页面的 frontmatter 校验）
    │
    ├── content/               # ★★ 全部「内容」放这里
    │   ├── posts/             #   博客文章（.md / .mdx）—— 写文章就在这
    │   │   └── examples/      #   示例文章目录
    │   └── pages/             #   独立页面内容，如 about.md
    │
    ├── pages/                 # ★ 路由页面（URL 结构由这里的文件决定）
    │   ├── index.astro        #   首页
    │   ├── about.astro        #   「关于」页面（渲染 content/pages/about.md）
    │   ├── search.astro       #   搜索页
    │   ├── 404.astro          #   404 页面
    │   ├── archives/          #   归档页
    │   ├── posts/[...slug]/   #   文章详情页模板
    │   ├── tags/              #   标签页
    │   ├── rss.xml.ts         #   RSS 订阅生成
    │   └── robots.txt.ts      #   robots.txt 生成
    │
    ├── layouts/               # ★ 页面骨架（<head>、整体布局）
    │   ├── Layout.astro       #   全站基础布局（含字体、SEO meta、主题脚本）
    │   └── PostLayout.astro   #   文章详情页布局
    │
    ├── components/            # ★ 可复用 UI 组件
    │   ├── Header.astro       #   顶部导航栏
    │   ├── Footer.astro       #   页脚
    │   ├── Card.astro         #   文章卡片
    │   ├── Pagination.astro   #   分页
    │   ├── Socials.astro      #   社交图标
    │   ├── Tag.astro          #   标签
    │   └── ...                #   其它组件
    │
    ├── styles/                # ★★ 样式文件（改外观就在这）
    │   ├── global.css         #   全局基础样式
    │   ├── theme.css          #   ★ 主题颜色变量（深/浅色）+ 字体变量
    │   └── typography.css     #   正文排版样式
    │
    ├── i18n/                  # ★ 界面文字（多语言/文案）
    │   └── lang/en.ts         #   英文界面文字（导航、按钮等文案在此）
    │
    ├── assets/                # 会被 Astro 处理优化的资源
    │   ├── images/            #   图片
    │   ├── icons/             #   图标
    │   └── fonts/             #   ★ 本地字体文件（见第 10 节）
    │
    ├── scripts/               # 客户端脚本（如主题切换 theme.ts）
    └── utils/                 # 工具函数
```

> 记忆口诀：
> - **改文字内容** → `src/content/`
> - **改外观样式** → `src/styles/`
> - **改站点配置** → `astro-paper.config.ts`
> - **改界面文案** → `src/i18n/lang/en.ts`
> - **改页面结构/布局** → `src/pages/`、`src/layouts/`、`src/components/`

---

## 4. 「我想改 XX，去哪个文件」速查表

| 我想改… | 去哪里改 |
| --- | --- |
| 网站标题、描述、作者名 | [`astro-paper.config.ts`](astro-paper.config.ts) 的 `site` 部分 |
| 社交链接（GitHub/微博/邮箱等） | [`astro-paper.config.ts`](astro-paper.config.ts) 的 `socials` |
| 每页显示几篇文章 | [`astro-paper.config.ts`](astro-paper.config.ts) 的 `posts.perPage` |
| 开关深浅色 / 搜索 / 归档等功能 | [`astro-paper.config.ts`](astro-paper.config.ts) 的 `features` |
| 写一篇新文章 | 在 `src/content/posts/` 新建 `.md` 文件（见第 5 节） |
| 改「关于」页面内容 | `src/content/pages/about.md` |
| 主题颜色（深色/浅色配色） | `src/styles/theme.css` |
| 正文字体大小、行距等排版 | `src/styles/typography.css` |
| 全局通用样式 | `src/styles/global.css` |
| 导航栏文字（Home/Posts/Tags…） | `src/i18n/lang/en.ts` 的 `nav` |
| 「上一篇/下一篇」「分享」等按钮文字 | `src/i18n/lang/en.ts` 对应分组 |
| 顶部导航栏结构 | `src/components/Header.astro` |
| 页脚 | `src/components/Footer.astro` |
| 网站图标 favicon | `public/favicon.svg` |
| 默认社交分享大图 | `public/default-og.jpg` |
| 字体 | `astro.config.ts` + `src/assets/fonts/`（见第 10 节） |

---

## 5. 写文章 / 改文章

### 5.1 新建文章

在 **`src/content/posts/`** 目录下新建一个 `.md`（普通 Markdown）或 `.mdx`（可用组件的 Markdown）文件即可。

文件顶部必须写 **frontmatter**（YAML 元数据），示例：

```markdown
---
title: 我的第一篇文章
description: 这是文章摘要，用于列表展示与 SEO。
pubDatetime: 2026-08-24T10:00:00Z
author: 你的名字
featured: false
draft: false
tags:
  - 随笔
  - 技术
---

这里开始写正文，支持标准 Markdown 语法。

## 二级标题

正文段落……
```

### 5.2 frontmatter 字段说明

| 字段 | 含义 | 是否必填 | 默认值 |
| --- | --- | --- | --- |
| `title` | 文章标题（页面 h1） | **必填** | — |
| `description` | 摘要（列表展示 + SEO） | **必填** | — |
| `pubDatetime` | 发布时间（ISO 8601 格式） | **必填** | — |
| `modDatetime` | 修改时间（改文章时才加） | 可选 | — |
| `author` | 作者 | 可选 | 站点配置里的作者 |
| `featured` | 是否在首页「精选」区展示 | 可选 | `false` |
| `draft` | 是否为草稿（草稿不会发布） | 可选 | `false` |
| `tags` | 标签（YAML 数组） | 可选 | `["others"]` |
| `ogImage` | 社交分享图（远程 URL 或相对路径） | 可选 | 站点默认图 / 自动生成 |
| `canonicalURL` | 规范链接（文章原发在别处时用） | 可选 | 自动生成 |
| `hideEditPost` | 隐藏本文的「编辑」按钮 | 可选 | `false` |
| `timezone` | 本文使用的时区（IANA 格式） | 可选 | 站点时区 |

> 💡 获取 ISO 8601 时间：在浏览器控制台运行 `new Date().toISOString()`。

### 5.3 文件路径与 URL 的关系

- `src/content/posts/very-first-post.md` → `/posts/very-first-post`
- `src/content/posts/2025/example.md` → `/posts/2025/example`（子目录会进入 URL）
- **以 `_` 开头的文件或目录会被忽略**，不会生成页面 —— 适合放草稿、内部资料、共享图片。

### 5.4 文章里放图片

- **本地图片**：把图片放到文章同级目录，Markdown 里用相对路径引用，Astro 会自动优化。
- **也可**直接用远程图片 URL。

---

## 6. 改「关于」等独立页面

「关于」页面的**文字内容**在 `src/content/pages/about.md`，直接编辑其中的 Markdown 即可。

它的 frontmatter 只需要 `title`（`description`、`ogImage`、`canonicalURL` 可选）。

页面的**布局/结构**（不常改）在 `src/pages/about.astro`。

---

## 7. 改站点信息、导航、社交链接

打开 [`astro-paper.config.ts`](astro-paper.config.ts)，常改的几块：

```ts
site: {
  url: "https://你的域名/",     // 站点正式地址（影响 SEO、RSS、sitemap）
  title: "站点标题",
  description: "站点描述",
  author: "作者名",
  lang: "en",                    // 语言标记
  timezone: "Asia/Bangkok",      // 站点时区，中国可改 "Asia/Shanghai"
},

posts: {
  perPage: 4,                    // 列表页每页文章数
},

features: {
  lightAndDarkMode: true,        // 深浅色切换
  showArchives: true,            // 归档页开关
  search: "pagefind",            // 站内搜索
  // ...
},

socials: [                       // 社交链接（图标会出现在首页/页脚）
  { name: "github",   url: "https://github.com/你的账号" },
  { name: "mail",     url: "mailto:你的邮箱" },
  // 按需增删
],
```

> 改完站点配置后，若开着 `pnpm dev`，Astro 会检测到配置变化并自动重启。

---

## 8. 改界面文字（导航栏 / 按钮等）

界面上的固定文案（不是文章内容）在 **`src/i18n/lang/en.ts`**。例如把导航栏改成中文：

```ts
nav: {
  home: "首页",
  posts: "文章",
  tags: "标签",
  about: "关于",
  archives: "归档",
  search: "搜索",
},
```

同理，`post`（上一篇/下一篇/分享等）、`pagination`（分页）、`footer`（页脚）、`notFound`（404 文案）等分组都可按需改成中文。

> ⚠️ 只改**值**（引号里的文字），不要改**键名**（冒号左边），否则页面会报错。

---

## 9. 改样式、主题颜色、深浅色

### 9.1 主题配色（最常改）

`src/styles/theme.css` 用 CSS 变量定义了浅色和深色两套配色：

```css
/* 浅色主题 */
:root,
[data-theme="light"] {
  --background: #fdfdfd;   /* 背景色 */
  --foreground: #282728;   /* 文字色 */
  --accent: #006cac;       /* 强调色（链接、按钮） */
  /* ... */
}

/* 深色主题 */
[data-theme="dark"] {
  --background: #212737;
  --foreground: #eaedf3;
  --accent: #ff6b01;
  /* ... */
}
```

改这几个十六进制颜色值即可换配色，深浅色分别改各自区块。

### 9.2 其它样式

- 正文排版（字号、行距、标题样式）：`src/styles/typography.css`
- 全局基础样式：`src/styles/global.css`
- 项目用的是 **Tailwind CSS v4**，组件里的 class（如 `text-foreground`、`bg-background`）会引用上面的颜色变量。

---

## 10. 字体说明（已改为本地离线字体）

> **本节记录 2026-08-24 做的一次重要改动，方便日后维护。**

### 10.1 背景

原主题使用 Astro 的 Google Fonts 在线字体（`Google Sans Code`），**每次启动/构建都要访问 `fonts.google.com`**。由于本地网络无法访问 Google，导致：

- 启动很慢（要等超时重试，约 60 秒）；
- 报警告 `[WARN] No data found for font family Google Sans Code`；
- 字体退回到系统默认，显示不稳定。

### 10.2 已做的修复：改为本地自托管字体

1. 从官方仓库 [googlefonts/googlesans-code](https://github.com/googlefonts/googlesans-code/releases) 下载了字体（v7.001），字体文件放在：
   ```text
   src/assets/fonts/
   ├── GoogleSansCode-VF.ttf          # 正体（可变字体，字重 300~800）
   ├── GoogleSansCode-Italic-VF.ttf   # 斜体（可变字体，字重 300~800）
   └── OFL.txt                        # 字体开源许可证（SIL OFL）
   ```

2. 修改了 [`astro.config.ts`](astro.config.ts) 的 `fonts` 配置，把 `fontProviders.google()` 换成 `fontProviders.local()`，并用 `options.variants` 指向本地文件。

3. 修改了 `src/layouts/Layout.astro` 里 `<Font>` 组件的预加载参数，字重从 `400` 改为可变字体区间 `"300 800"`。

**效果**：完全离线，启动时间从约 60 秒降到约 4 秒，不再有任何 Google 字体请求和警告。

### 10.3 以后想换字体怎么办

- **换成另一款本地字体**：把新字体文件（推荐 `.woff2` 或 `.ttf`）放进 `src/assets/fonts/`，再改 `astro.config.ts` 里 `options.variants` 的 `src` 路径、`weight`、`style`。
- **想直接用系统等宽字体**（最省事）：可把 `astro.config.ts` 的整个 `fonts` 段删除或注释，同时把 `src/styles/theme.css` 里的
  ```css
  --font-app: var(--font-google-sans-code);
  ```
  改成例如：
  ```css
  --font-app: ui-monospace, "Cascadia Code", "Consolas", monospace;
  ```
- **恢复 Google 在线字体**（需能访问 Google）：把 `astro.config.ts` 的 `provider` 改回 `fontProviders.google()`，并用 `weights: [...]` / `styles: [...]` 写法（不是 `options.variants`）。

---

## 11. 构建与部署

### 11.1 生产构建

```bash
pnpm build
```

产物在 `dist/` 目录，把 `dist/` 里的内容部署到任意静态托管（Nginx、Vercel、Netlify、GitHub Pages 等）即可。

> ⚠️ **Windows 注意**：`package.json` 里的 `build` 脚本最后一段是
> `cp -r dist/pagefind public/`，这是 **Linux/macOS 命令**，在 Windows PowerShell 下会报错。
> 两种处理办法：
> 1. 在 **Git Bash** 里运行 `pnpm build`（Git Bash 支持 `cp` 命令）；
> 2. 或把该脚本里的 `cp -r dist/pagefind public/` 改成 PowerShell 写法
>    `Copy-Item -Recurse -Force dist/pagefind public/`。
>
> （`pagefind` 是站内搜索的索引，这一步是把搜索索引拷进 public。）

### 11.2 上线前记得改的配置

- `astro-paper.config.ts` 里的 `site.url` 改成你的**正式域名**（影响 RSS、sitemap、canonical、OG）。

### 11.3 Docker（可选）

项目自带 `Dockerfile` 和 `compose.yaml`，如需容器化部署，可用：

```bash
docker compose up -d
```

---

## 12. 常见问题 FAQ

**Q1：启动很慢、卡在拉字体？**
已在第 10 节修复为本地字体。若又出现类似情况，检查 `astro.config.ts` 是否被改回了在线 `fontProviders.google()`。

**Q2：改了配置文件页面没变化？**
- 若开着 `pnpm dev`，通常会自动重启并热更新；若没生效，`Ctrl+C` 停掉后重新 `pnpm dev`。
- 改了内容 schema 类型报错时，运行一次 `pnpm sync`。

**Q3：新写的文章不显示？**
- 检查 frontmatter 是否写全了 `title`、`description`、`pubDatetime` 三个必填项；
- 检查文件名或所在目录是否以 `_` 开头（会被忽略）；
- 检查 `draft` 是否被设为 `true`（草稿不发布）；
- 检查 `pubDatetime` 是否是「未来时间」（未到发布时间的文章默认不显示）。

**Q4：`pnpm build` 在 Windows 报 `cp` 命令找不到？**
见第 11.1 节的说明，用 Git Bash 运行，或改脚本为 PowerShell 的 `Copy-Item`。

**Q5：端口 4321 被占用？**
用 `pnpm dev --port 5000` 指定其它端口。

---

*文档结束。日常最常打交道的三个位置：写文章 → `src/content/posts/`；改配置 → `astro-paper.config.ts`；改配色 → `src/styles/theme.css`。*
